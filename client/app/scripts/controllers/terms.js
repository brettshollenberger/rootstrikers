angular
  .module('app')
  .controller('termsController', [
    '$scope',
    '$routeParams',
    'userService',
    '$rootScope',
    function($scope, $routeParams, userService, $rootScope) {
      
    }]);
