angular
  .module('app')
  .controller('privacyController', [
    '$scope',
    '$routeParams',
    'userService',
    '$rootScope',
    function($scope, $routeParams, userService, $rootScope) {
      
    }]);
